-@ val isInvalidEmail: Boolean = false
- attributes("title") = "Scalavatar - upload avatar"
:javascript
  $(document).ready(function(){
    $('input[type=file]').bootstrapFileInput();
  });
.container
  .col-md-6
    h1 Scalavatar
  .col-md-4
    h2 Upload Image File
    form(action="/avatar" method="post" enctype="multipart/form-data")
      - val email_alert = if (isInvalidEmail) "has-error has-feedback" else ""
      div(class="#{email_alert}")
        p
          input.form-control#email(type="text" name="e-mail" placeholder="E-mail address")
        - if (isInvalidEmail)
          p.text-danger Invalid e-mail address.
      div
        p
          input(type="file" name="image-file")
      div
        p
          button.btn.btn-primary Send
